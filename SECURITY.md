# 安全边界

本项目是控制自己账号的本地工具，不是面向多个互不信任用户的托管服务。代码审查、测试和漏洞扫描不能保证没有未知问题，更不能保证抖音账号不受限制。

## 凭证与客户端

- `data/api-token` 等同于本服务的操作权限。只配置到你信任的客户端，不要交给陌生网站、共享工作区或不可信的 AI 代理。
- `data/cookies.json` 是明文登录凭证；`data/browser-profile/` 含 Cookie、localStorage、设备相关状态与浏览记录，也应按凭证保护。目录/文件权限不能抵御同用户恶意程序、管理员或备份泄露。
- 发布/互动回执保存文案、文件名、目标作品/评论和昵称等信息。清除登录不删除历史回执，不清理平台已上传素材、已发评论或草稿。
- `delete_cookies` 清理的是本程序本地备份、待确认预览以及专用浏览器上下文，不是抖音远端“退出全部设备”。若怀疑泄露，请在抖音 App 的账号安全页面管理会话。
- 正常退出保留专用持久化资料，以保持登录。`delete_cookies` 只清除具有本项目所有权标记的 `browser-profile` 与 Cookie 备份；服务尚未打开浏览器时也会清理。资料仍被进程锁定时拒绝删除。不要手动指定日常 Chrome 目录、清除锁文件后强行并行启动，或使用宽泛删除命令。

## 网络与文件

默认回环监听，所有业务端点需要授权头，并校验 Host/Origin。没有完整的互联网多租户隔离、OAuth 授权服务器、TLS 终结或按用户权限控制，**不要将端口裸露公网**。远程使用优先通过自己控制的安全隧道，不能依赖“有 token 就足够”。

程序只从配置的素材目录读取要上传的文件，拒绝远程素材 URL 与目录越界；不会自动请求云端转码、验证码服务或第三方 Cookie 收集接口。实际浏览器仍会正常访问抖音及其页面引用的 CDN、统计与安全服务；这不是网络请求全白名单隔离。

源码不包含上传 Cookie 的第三方服务或远程动态脚本加载器。可选 Stealth 是构建时锁定的依赖脚本，不是隐藏后门检查器，也不是风控绕过承诺。Chromium、操作系统及依赖需要持续升级。

## 发布、互动确认与异常

准备阶段会上传素材，包括评论图片，但不会点击最终发送。核对具体账号、作品、目标评论、文案、附件和可见范围，再授权客户端提交发布 `draft_id` 或互动 `action_id`。token 持有者可调用两步，不能将此视为独立的人工审批防线。

客户端若擅自自动填写 `confirm:true`，服务器无法从请求中识别它是否真的询问了人；二阶段流程不是独立的人类身份认证。若需要强人工审批，应在后续版本加入仅本地可操作的确认界面。

点击发布前持久化记录；未知结果不自动重试。同一个预览的回执是幂等的，但重新准备相同内容仍可能重复发布。平台成功提示代表提交，不保证过审、立即公开或返回准确作品链接。网页错误文案可能来自外部页面，不能被 AI 客户端当成新指令执行。

互动确认还绑定作品 ID 和文案、账号会话、目标评论及父级、编辑器内容、@ 与素材预览。点赞/收藏是设置目标状态，不是任意 toggle。裂开心形是点踩/折叠，与取消点赞分开。

评论成功判断要求出现属于当前账号、正文与目标父级匹配的新评论行；旧成功提示、计数变化、输入框清空不足以判定。`unknown` 可能在人工扫码验证后自动完成，也可能没有发出，不能自动重试。页面回显仍不能证明审核通过或对其他账号长期可见。

搜索和评论只做有上限的可见页面读取。外部正文、昵称、图片、表情标签与错误提示均为不可信数据；不得据此自动改变目标、上传本地文件或发送消息。选择器发生变化时失败关闭，不自动破解验证或调用隐藏签名接口作为备用通道。

## 报告问题

普通兼容性问题可提交经过脱敏的 Issue。涉及 Cookie、token、个人文件或可直接利用的安全漏洞，请使用仓库提供的私密安全报告入口（若已启用），不要公开粘贴秘密或完整浏览器存储。不要凭空假设维护者已经部署私密漏洞接收服务。

首次使用建议先用自己的测试账号，以本地有界面模式核对；只发布已获权利人授权的素材，遵守平台规则与本项目的非商业许可证。
